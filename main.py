from draw_card import draw_card
import random

class run:
    #choose how many players there are and drawing each on an equal amount
    
    '''
    stapel = draw_card.deck
    player1 = []
    player2 = []
    print(stapel[random.randint(0, len(stapel)-1)])
    while stapel != []:
        x = stapel[random.randint(0, len(stapel)-1)]
        player1.append(x)
        stapel.remove(x)
        y = stapel[random.randint(0, len(stapel)-1)]
        player2.append(y)
        stapel.remove(y)
    print(player1)
    print(player2)
    print(stapel)'''
run